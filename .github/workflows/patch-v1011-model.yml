name: Patch V10.1.1 model

on:
  push:
    branches: [agent/v10-full-joint-model-rebuild]

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[v1011-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v10-full-joint-model-rebuild
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/patch-v1011-model.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app lib scripts package.json DEPLOYMENT_VERSION
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "fix: complete standalone exact model v10.1.1 [v1011-applied]"
            git push origin HEAD:agent/v10-full-joint-model-rebuild
          fi
