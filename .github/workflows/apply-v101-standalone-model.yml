name: Apply V10.1 standalone model

on:
  push:
    branches: [agent/v10-full-joint-model-rebuild]

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[v101-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v10-full-joint-model-rebuild
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply integration patch
        run: node scripts/apply-v101-standalone-model.mjs
      - name: Commit generated integration changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app lib package.json DEPLOYMENT_VERSION
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "feat: integrate standalone exact joint model v10.1 [v101-applied]"
            git push origin HEAD:agent/v10-full-joint-model-rebuild
          fi
