name: Apply v9.2 Safari hydration safety

on:
  push:
    branches: [feature/one-click-reference-board-v9-2]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-build-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: python scripts/apply-v92-hydration-safe-store.py
      - run: npm install --no-audit --no-fund --package-lock=false
      - run: npm run build
      - name: Commit verified hydration-safe source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f .github/workflows/apply-v92-hydration-safe-store.yml scripts/apply-v92-hydration-safe-store.py
          git add app/page.js
          git commit -m 'Make v9.2 client hydration deterministic on Safari'
          git push origin HEAD:feature/one-click-reference-board-v9-2
